function findMaxNumber(a, b) {
    return a > b ? a : b;
}

// Example usage
const num1 = 287;
const num2 = 189;
const maxNumber = findMaxNumber(num1, num2);
console.log("The maximum number is:", maxNumber);
